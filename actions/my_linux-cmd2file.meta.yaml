---
# Action definition metadata
name: "my_linux-cmd2file"
description: "Action Chain workflow passing stdout to file"
# `runner_type` identifies the runner
runner_type: "action-chain"
# `entry_point` path to the ActionChain definition file, relative to the pack's action directory.
entry_point: "chains/stdout2file.yaml"
enabled: true